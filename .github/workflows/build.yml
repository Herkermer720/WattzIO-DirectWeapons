name: Build Direct Weapon Swap

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Test GitHub Actions
        shell: pwsh
        run: |
          Write-Host "GitHub Actions Windows runner is working."
          Write-Host "Direct Weapon Swap build pipeline is ready."
          Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion
